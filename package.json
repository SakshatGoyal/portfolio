{
  "name": "sakshat-portfolio-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check:dividers": "node scripts/check-divider-ownership.mjs",
    "build": "npm run check:dividers && astro check && astro build",
    "preview": "astro preview",
    "check": "npm run check:dividers && astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.13.5",
    "typescript": "^5.9.2"
  }
}
